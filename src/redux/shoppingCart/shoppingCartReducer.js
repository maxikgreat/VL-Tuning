
const initialState = {
   items: [],
   total: 0
}

export default function shoppingCartReducer(state = initialState, {type, payload}){
    switch(type){
        default: 
            return state
    }
}

